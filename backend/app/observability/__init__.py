"""OpenTelemetry-compatible instrumentation (Milestone 10C).

See `app.observability.tracing`'s module docstring for the correlation-ID
decision (`document_extraction.id`, not a persisted W3C `traceparent`).
"""

from __future__ import annotations
