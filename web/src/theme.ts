import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#1F3A5F" },
    secondary: { main: "#5C6773" },
  },
  shape: { borderRadius: 6 },
});
