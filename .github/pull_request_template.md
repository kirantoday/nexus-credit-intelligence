## Summary

What this PR does, in a sentence or two.

## Milestone

Which `PLAN.md` § Milestone Status row this belongs to (or "hardening" / "N/A" for
non-milestone work).

## Changes

-

## Testing

- [ ] `pytest` passes (backend)
- [ ] `ruff check` / `black --check` / `mypy` pass (backend)
- [ ] `npm run lint` / `npm run typecheck` / `npm run format:check` pass (frontend)
- [ ] `npm run build` succeeds (frontend)
- [ ] `pre-commit run --all-files` passes
- [ ] Manually verified: <what you actually clicked/ran>

## Provenance / entitlement impact

- [ ] N/A — no new displayed facts, calculated values, or licensed data paths
- [ ] New displayed facts carry a `provenance` record
- [ ] New calculated values retain calculation lineage
- [ ] New licensed-data paths go through `policy_check`
- [ ] New synthetic data is tagged `SYNTHETIC_DEMO_DATA`

## Architecture

- [ ] No architecture change — fits within the frozen Version 1.0 architecture
- [ ] Architecture change — linked to an **Architecture change** issue/ADR:

## Documentation

- [ ] `PLAN.md` updated (Project Status / Milestone Status / Technical Debt / Known
      Issues / Next Immediate Goal) if this completes or changes a milestone
- [ ] `BUILD_LOG.md` entry appended if this completes a milestone
- [ ] `ARCHITECTURE_DECISIONS.md` entry appended if this is a significant
      architecture decision (not required for ordinary changes)
